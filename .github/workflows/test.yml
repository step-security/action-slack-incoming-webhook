name: "units-test"
on:
  pull_request:
  push:
    branches:
      - master
      - test

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - run: npm ci
    - run: npm test
